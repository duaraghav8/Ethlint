import 'file';
import 'file.sol';
import 'a.sol';
import 'x';
import '  o';
import * as sym from 'file';
import {a, b} from 'file.sol';
import '../myContract.sol' as MyContract;

contract Foo {
        string fucker = 'hello wordl';
        function f () {
                var foobar = 'Hello world';
                string fuu = 'chumma';
        }
}