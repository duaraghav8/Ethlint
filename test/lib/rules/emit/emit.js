/**
 * @fileoverview Tests for emit rule
 * @author Raghav Dua <duaraghav8@gmail.com>
 */

"use strict";

const Solium = require("../../../../lib/solium"),
    { toContract, toFunction } = require("../../../utils/wrappers");
const userConfig = { "rules": { "emit": true } };


describe("[RULE] emit: Acceptances", () => {

    it("should accept code without any event declarations", done => {
        done();
    });

    it("should", done => {
        done();
    });

    it("should", done => {
        done();
    });

    it("should", done => {
        done();
    });

    it("should", done => {
        done();
    });

    it("should", done => {
        done();
    });

    it("should", done => {
        done();
    });

});


describe("[RULE] emit: Rejections", () => {

    it("should", done => {
        done();
    });

    it("should", done => {
        done();
    });

    it("should", done => {
        done();
    });

    it("should", done => {
        done();
    });

    it("should", done => {
        done();
    });

    it("should", done => {
        done();
    });

    it("should", done => {
        done();
    });

});